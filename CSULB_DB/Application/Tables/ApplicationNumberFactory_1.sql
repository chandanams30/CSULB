CREATE TABLE [Application].[ApplicationNumberFactory] (
    [ID]                INT IDENTITY (1, 1) NOT NULL,
    [ProgramID]         INT NOT NULL,
    [SemesterID]        INT NOT NULL,
    [ApplicationNumber] INT NOT NULL
);

